let numChildren = Number(prompt("Please enter a number:"));
let isCareful = true;

// if (isCareful == false) {
//     numChildren += 1;
// }

// console.log(numChildren);

// ternary version

isCareful == false ? numChildren += 1 : numChildren = numChildren ;

console.log(numChildren);